# ────────────────────────────────────────────────────────────
# Codemagic CI/CD — iOS Builds for cliniq.one Capacitor Apps
# ────────────────────────────────────────────────────────────
# Each app builds its web source first, then bundles it into
# the native binary via `cap sync`. No server.url dependency.
# ────────────────────────────────────────────────────────────

workflows:
  # ─── DOCTOR iOS ─────────────────────────────────────────────
  doctor-ios:
    name: Doctor iOS
    instance_type: mac_mini_m2
    max_build_duration: 60
    integrations:
      app_store_connect: codemagic
    environment:
      xcode: latest
      node: 22
      groups:
        - supabase_credentials
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
      cancel_previous_builds: true
    scripts:
      - name: Install dependencies
        script: npm ci --legacy-peer-deps
      - name: Build doctor-web (static export)
        script: |
          cd apps/doctor-web
          CAPACITOR_BUILD=1 npx next build
          echo "=== doctor-web/out contents ==="
          ls -la out/ | head -20
      - name: Add and sync iOS platform
        script: |
          cd apps/doctor-capacitor
          ../../node_modules/.bin/cap add ios 2>&1 || echo "cap add finished (may already exist)"
          ../../node_modules/.bin/cap sync ios
          echo "=== iOS files generated ==="
          find ios -name "*.xcworkspace" -o -name "*.xcodeproj" 2>/dev/null | head -10
      - name: Debug - list iOS project structure
        script: |
          echo "=== iOS project structure ==="
          ls -la apps/doctor-capacitor/ios/App/ || echo "ios/App not found"
          echo "=== Looking for xcworkspace ==="
          find apps/doctor-capacitor/ios -name "*.xcworkspace" -o -name "*.xcodeproj" 2>/dev/null || echo "No xcode projects found"
      - name: Set up code signing
        script: |
          openssl genrsa -out /tmp/cert_key.pem 2048
          export CERTIFICATE_PRIVATE_KEY=$(cat /tmp/cert_key.pem)
          keychain initialize
          app-store-connect fetch-signing-files "com.cliniqone.doctor" \
            --type IOS_APP_STORE \
            --create
          keychain add-certificates
          xcode-project use-profiles
      - name: Patch Info.plist
        script: |
          PLIST="apps/doctor-capacitor/ios/App/App/Info.plist"
          /usr/libexec/PlistBuddy -c "Add :ITSAppUsesNonExemptEncryption bool false" "$PLIST" 2>/dev/null || \
          /usr/libexec/PlistBuddy -c "Set :ITSAppUsesNonExemptEncryption false" "$PLIST"
          /usr/libexec/PlistBuddy -c "Delete :UIRequiredDeviceCapabilities" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Add :UIRequiredDeviceCapabilities array" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :UIRequiredDeviceCapabilities:0 string arm64" "$PLIST"
          echo "=== Patched Info.plist ==="
          cat "$PLIST"
      - name: Build IPA
        script: |
          cd apps/doctor-capacitor/ios/App
          if [ -d "App.xcworkspace" ]; then
            xcode-project build-ipa \
              --workspace "App.xcworkspace" \
              --scheme "App"
          else
            xcode-project build-ipa \
              --project "App.xcodeproj" \
              --scheme "App"
          fi
    artifacts:
      - apps/doctor-capacitor/ios/App/build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        beta_groups:
          - Cliniq Testers
      slack:
        channel: '#general'
        notify_on_build_start: true
        notify:
          success: true
          failure: true

  # ─── ADMIN iOS ──────────────────────────────────────────────
  admin-ios:
    name: Admin iOS
    instance_type: mac_mini_m2
    max_build_duration: 60
    integrations:
      app_store_connect: codemagic
    environment:
      xcode: latest
      node: 22
      groups:
        - supabase_credentials
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
      cancel_previous_builds: true
    scripts:
      - name: Install dependencies
        script: npm ci --legacy-peer-deps
      - name: Build admin (static export)
        script: |
          cd apps/admin
          CAPACITOR_BUILD=1 npx next build
          echo "=== admin/out contents ==="
          ls -la out/ | head -20
      - name: Add and sync iOS platform
        script: |
          cd apps/admin-capacitor
          ../../node_modules/.bin/cap add ios 2>&1 || echo "cap add finished (may already exist)"
          ../../node_modules/.bin/cap sync ios
          echo "=== iOS files generated ==="
          find ios -name "*.xcworkspace" -o -name "*.xcodeproj" 2>/dev/null | head -10
      - name: Debug - list iOS project structure
        script: |
          echo "=== iOS project structure ==="
          ls -la apps/admin-capacitor/ios/App/ || echo "ios/App not found"
          echo "=== Looking for xcworkspace ==="
          find apps/admin-capacitor/ios -name "*.xcworkspace" -o -name "*.xcodeproj" 2>/dev/null || echo "No xcode projects found"
      - name: Set up code signing
        script: |
          openssl genrsa -out /tmp/cert_key.pem 2048
          export CERTIFICATE_PRIVATE_KEY=$(cat /tmp/cert_key.pem)
          keychain initialize
          app-store-connect fetch-signing-files "com.cliniqone.admin" \
            --type IOS_APP_STORE \
            --create
          keychain add-certificates
          xcode-project use-profiles
      - name: Patch Info.plist
        script: |
          PLIST="apps/admin-capacitor/ios/App/App/Info.plist"
          /usr/libexec/PlistBuddy -c "Add :ITSAppUsesNonExemptEncryption bool false" "$PLIST" 2>/dev/null || \
          /usr/libexec/PlistBuddy -c "Set :ITSAppUsesNonExemptEncryption false" "$PLIST"
          /usr/libexec/PlistBuddy -c "Delete :UIRequiredDeviceCapabilities" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Add :UIRequiredDeviceCapabilities array" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :UIRequiredDeviceCapabilities:0 string arm64" "$PLIST"
          echo "=== Patched Info.plist ==="
          cat "$PLIST"
      - name: Build IPA
        script: |
          cd apps/admin-capacitor/ios/App
          if [ -d "App.xcworkspace" ]; then
            xcode-project build-ipa \
              --workspace "App.xcworkspace" \
              --scheme "App"
          else
            xcode-project build-ipa \
              --project "App.xcodeproj" \
              --scheme "App"
          fi
    artifacts:
      - apps/admin-capacitor/ios/App/build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        beta_groups:
          - Cliniq Testers
      slack:
        channel: '#new-channel'
        notify_on_build_start: true
        notify:
          success: true
          failure: true

  # ─── PATIENT iOS ────────────────────────────────────────────
  patient-ios:
    name: Patient iOS
    instance_type: mac_mini_m2
    max_build_duration: 60
    integrations:
      app_store_connect: codemagic
    environment:
      xcode: latest
      node: 22
      groups:
        - supabase_credentials
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
      cancel_previous_builds: true
    scripts:
      - name: Install dependencies
        script: npm ci --legacy-peer-deps
      - name: Build patient web (Expo export)
        script: |
          cd apps/patient
          npx expo export --platform web
          echo "=== patient/dist contents ==="
          ls -la dist/ | head -20
      - name: Add and sync iOS platform
        script: |
          cd apps/patient-capacitor
          ../../node_modules/.bin/cap add ios 2>&1 || echo "cap add finished (may already exist)"
          ../../node_modules/.bin/cap sync ios
          echo "=== iOS files generated ==="
          find ios -name "*.xcworkspace" -o -name "*.xcodeproj" 2>/dev/null | head -10
      - name: Debug - list iOS project structure
        script: |
          echo "=== iOS project structure ==="
          ls -la apps/patient-capacitor/ios/App/ || echo "ios/App not found"
          echo "=== Looking for xcworkspace ==="
          find apps/patient-capacitor/ios -name "*.xcworkspace" -o -name "*.xcodeproj" 2>/dev/null || echo "No xcode projects found"
      - name: Set up code signing
        script: |
          openssl genrsa -out /tmp/cert_key.pem 2048
          export CERTIFICATE_PRIVATE_KEY=$(cat /tmp/cert_key.pem)
          keychain initialize
          app-store-connect fetch-signing-files "com.cliniqone.patient.cap" \
            --type IOS_APP_STORE \
            --create
          keychain add-certificates
          xcode-project use-profiles
      - name: Patch Info.plist
        script: |
          PLIST="apps/patient-capacitor/ios/App/App/Info.plist"
          /usr/libexec/PlistBuddy -c "Add :ITSAppUsesNonExemptEncryption bool false" "$PLIST" 2>/dev/null || \
          /usr/libexec/PlistBuddy -c "Set :ITSAppUsesNonExemptEncryption false" "$PLIST"
          /usr/libexec/PlistBuddy -c "Delete :UIRequiredDeviceCapabilities" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Add :UIRequiredDeviceCapabilities array" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :UIRequiredDeviceCapabilities:0 string arm64" "$PLIST"
          echo "=== Patched Info.plist ==="
          cat "$PLIST"
      - name: Build IPA
        script: |
          cd apps/patient-capacitor/ios/App
          if [ -d "App.xcworkspace" ]; then
            xcode-project build-ipa \
              --workspace "App.xcworkspace" \
              --scheme "App"
          else
            xcode-project build-ipa \
              --project "App.xcodeproj" \
              --scheme "App"
          fi
    artifacts:
      - apps/patient-capacitor/ios/App/build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        beta_groups:
          - Cliniq Testers
      slack:
        channel: '#new-channel'
        notify_on_build_start: true
        notify:
          success: true
          failure: true

  # ─── LOCUM iOS ──────────────────────────────────────────────
  locum-ios:
    name: Locum iOS
    instance_type: mac_mini_m2
    max_build_duration: 60
    integrations:
      app_store_connect: codemagic
    environment:
      xcode: latest
      node: 22
      groups:
        - supabase_credentials
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
      cancel_previous_builds: true
    scripts:
      - name: Install dependencies
        script: npm ci --legacy-peer-deps
      - name: Build doctor-web for locum (static export)
        script: |
          cd apps/doctor-web
          CAPACITOR_BUILD=1 npx next build
          echo "=== doctor-web/out contents (for locum) ==="
          ls -la out/ | head -20
      - name: Add and sync iOS platform
        script: |
          cd apps/locum-capacitor
          ../../node_modules/.bin/cap add ios 2>&1 || echo "cap add finished (may already exist)"
          ../../node_modules/.bin/cap sync ios
          echo "=== iOS files generated ==="
          find ios -name "*.xcworkspace" -o -name "*.xcodeproj" 2>/dev/null | head -10
      - name: Debug - list iOS project structure
        script: |
          echo "=== iOS project structure ==="
          ls -la apps/locum-capacitor/ios/App/ || echo "ios/App not found"
          echo "=== Looking for xcworkspace ==="
          find apps/locum-capacitor/ios -name "*.xcworkspace" -o -name "*.xcodeproj" 2>/dev/null || echo "No xcode projects found"
      - name: Set up code signing
        script: |
          openssl genrsa -out /tmp/cert_key.pem 2048
          export CERTIFICATE_PRIVATE_KEY=$(cat /tmp/cert_key.pem)
          keychain initialize
          app-store-connect fetch-signing-files "com.cliniqone.locum" \
            --type IOS_APP_STORE \
            --create
          keychain add-certificates
          xcode-project use-profiles
      - name: Patch Info.plist
        script: |
          PLIST="apps/locum-capacitor/ios/App/App/Info.plist"
          /usr/libexec/PlistBuddy -c "Add :ITSAppUsesNonExemptEncryption bool false" "$PLIST" 2>/dev/null || \
          /usr/libexec/PlistBuddy -c "Set :ITSAppUsesNonExemptEncryption false" "$PLIST"
          /usr/libexec/PlistBuddy -c "Delete :UIRequiredDeviceCapabilities" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Add :UIRequiredDeviceCapabilities array" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :UIRequiredDeviceCapabilities:0 string arm64" "$PLIST"
          echo "=== Patched Info.plist ==="
          cat "$PLIST"
      - name: Build IPA
        script: |
          cd apps/locum-capacitor/ios/App
          if [ -d "App.xcworkspace" ]; then
            xcode-project build-ipa \
              --workspace "App.xcworkspace" \
              --scheme "App"
          else
            xcode-project build-ipa \
              --project "App.xcodeproj" \
              --scheme "App"
          fi
    artifacts:
      - apps/locum-capacitor/ios/App/build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        beta_groups:
          - Cliniq Testers
      slack:
        channel: '#general'
        notify_on_build_start: true
        notify:
          success: true
          failure: true
